import React from 'react';
// import PropTypes from 'prop-types';
import { BarIndicator } from 'react-native-indicators';

const Spinner = props => (
  <BarIndicator color='white' count={7} />
);

// Spinner.propTypes = {};

export default Spinner;